"Kill the white bar at the bottom of the window.
set laststatus=1

"I don't like sidescrolloff. When I'm editing in an 80-character terminal,
"it makes the screen jump suddenly, just because I typed a 76-character line.
"vim-sensible would set this by default if it's unset.
set sidescrolloff=1
set sidescroll=4
